import streamlit as st
import pandas as pd

# إعداد واجهة التطبيق
st.set_page_config(page_title="Radiology Tasks", page_icon="🩻")

st.title("🏥 نظام إدارة مهام قسم الأشعة")
st.write("مرحباً بك! هذا النظام مصمم لتنظيم سير العمل داخل القسم.")

# إضافة المهمة
with st.expander("➕ إضافة مهمة جديدة للقسم"):
    t_name = st.text_input("اسم المهمة (مثلاً: صيانة جهاز MRI)")
    t_member = st.selectbox("تعيين لزميل", ["أحمد", "سارة", "خالد", "محمد"])
    if st.button("اعتماد المهمة"):
        st.success(f"تم إسناد المهمة لـ {t_member} بنجاح!")

# جدول المتابعة
st.subheader("📊 لوحة متابعة المهام الحالية")
# هنا سنقوم لاحقاً بربط قاعدة البيانات